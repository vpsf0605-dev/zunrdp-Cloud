name: 🌐 ZUNRDP Cloud

on:
  workflow_dispatch:
    inputs:
      machine_name:
        description: 'Chon He Dieu Hanh'
        required: true
        type: choice
        default: 'Windows'
        options: [Windows, Ubuntu]
      owner_name:
        description: 'Nhap Ten Nguoi Tao (Hien tren Web)'
        required: true
        default: 'Admin'

jobs:
  setup-vm:
    runs-on: ${{ github.event.inputs.machine_name == 'Windows' && 'windows-latest' || 'ubuntu-latest' }}
    timeout-minutes: 360
    steps:
      - name: 1. LAY MA NGUON
        uses: actions/checkout@v4

      - name: 2. CAI TAILSCALE (WINDOWS)
        if: github.event.inputs.machine_name == 'Windows'
        shell: powershell
        run: |
          $msi = "$env:TEMP\tailscale.msi"
          Invoke-WebRequest "https://pkgs.tailscale.com/stable/tailscale-setup-latest-amd64.msi" -OutFile $msi
          Start-Process msiexec.exe -ArgumentList "/i", $msi, "/quiet", "/norestart" -Wait
          Start-Sleep -Seconds 10

      - name: 3. CAU HINH RDP (WINDOWS)
        if: github.event.inputs.machine_name == 'Windows'
        shell: powershell
        run: |
          net user ADMINZUN ZunRDP@123456 /add
          net localgroup administrators ADMINZUN /add
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -name "fDenyTSConnections" -value 0
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop"

      - name: 4. CHAY AGENT (WINDOWS)
        if: github.event.inputs.machine_name == 'Windows'
        shell: powershell
        env:
          TS_KEY: ${{ secrets.TAILSCALE_AUTH_KEY }}
          VM_OWNER: ${{ github.event.inputs.owner_name }}
        run: .\run_vm.ps1

      - name: 5. CAI DAT MOI TRUONG (UBUNTU)
        if: github.event.inputs.machine_name == 'Ubuntu'
        shell: bash
        run: |
          sudo apt-get update
          sudo apt-get install -y xrdp
          curl -fsSL https://tailscale.com/install.sh | sh

      - name: 6. CHAY AGENT (UBUNTU)
        if: github.event.inputs.machine_name == 'Ubuntu'
        shell: bash
        env:
          TS_KEY: ${{ secrets.TAILSCALE_AUTH_KEY }}
          VM_OWNER: ${{ github.event.inputs.owner_name }}
        run: |
          chmod +x run_ubuntu.sh
          ./run_ubuntu.sh

